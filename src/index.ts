/**
 * PRIVA-FISC — circuit entry point (PLACEHOLDER).
 *
 * No circuit logic yet. Implementation begins ONLY after `spec/SPEC.md` is
 * drafted and approved.
 *
 * v0 scope headline = the SIGNATURE-BINDING architecture, demonstrated on
 * SYNTHETIC data. This is NOT real in-circuit RSA verification of Montenegro's
 * fiscal signature chain — that is explicitly Phase-5 / R&D.
 *
 * See ../README.md (honest scope + data policy) and ../spec/SPEC.md (pending).
 */

export {};
