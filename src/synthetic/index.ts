/**
 * Synthetic EFI fiscal-receipt + certificate generator (PLACEHOLDER).
 *
 * Generates SYNTHETIC, schema-shaped test data ONLY. No real PII, no real
 * taxpayer certificates, no real Tax Administration keys — ever.
 *
 * It will mirror the Montenegro EFI XML receipt shape and a synthetic-format
 * RSA key/cert, so the PoC can exercise the signature-binding architecture
 * without touching any real data.
 *
 * Implementation pending `spec/SPEC.md`.
 */

export {};
