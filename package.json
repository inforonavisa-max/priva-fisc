{
  "name": "priva-fisc-poc",
  "version": "0.0.0",
  "description": "PRIVA-FISC — zero-knowledge selective-disclosure proof of fiscalization. v0 PoC on Mina (o1js, TypeScript). Synthetic data only; no real PII/keys.",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.14.0"
  },
  "scripts": {
    "build": "tsc",
    "buildw": "tsc --watch",
    "gen": "tsc && node build/src/index.js",
    "check": "tsc && node build/scripts/check.js",
    "test": "tsc && node build/test/runner.js",
    "test:circuit": "tsc && node build/test/t-circuit.test.js"
  },
  "dependencies": {
    "o1js": "2.15.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tslib": "^2.8.1",
    "typescript": "^5.9.3"
  }
}
