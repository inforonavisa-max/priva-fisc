{
  "name": "priva-fisc-poc",
  "version": "0.0.0",
  "description": "PRIVA-FISC — zero-knowledge selective-disclosure proof of fiscalization. v0 PoC on Mina (o1js, TypeScript). Synthetic data only; no real PII/keys.",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.14.0"
  },
  "scripts": {
    "build": "tsc",
    "buildw": "tsc --watch",
    "test": "echo \"No tests yet — circuit implementation pending spec/SPEC.md approval.\" && exit 0"
  },
  "dependencies": {
    "o1js": "^2.15.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tslib": "^2.8.1",
    "typescript": "^5.9.3"
  }
}
